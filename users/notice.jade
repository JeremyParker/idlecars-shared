.form-container
  .text-wrapper
    section
      label(for="{label}}") {{label}}

    section
      div(ng-repeat="content in contents track by $index")
        .content {{content}}

    section
      div(ng-repeat="button in buttons track by $index")
        button.button(ng-click="button['click']()") {{button.value}}
